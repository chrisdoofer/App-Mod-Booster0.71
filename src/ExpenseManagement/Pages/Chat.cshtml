@page
@model ExpenseManagement.Pages.ChatModel
@{
    ViewData["Title"] = "AI Chat Assistant";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0">
                        <i class="bi bi-robot"></i> AI Expense Assistant
                    </h3>
                    <p class="mb-0 small">Ask me anything about expenses or request actions</p>
                </div>
                <div class="card-body p-0">
                    @if (!Model.GenAIEnabled)
                    {
                        <div class="alert alert-warning m-3" role="alert">
                            <h5 class="alert-heading"><i class="bi bi-exclamation-triangle"></i> GenAI Not Configured</h5>
                            <p>The chat functionality requires Azure OpenAI to be deployed.</p>
                            <hr>
                            <p class="mb-0">
                                To enable chat features, redeploy the infrastructure with the <code>-DeployGenAI</code> switch:
                            </p>
                            <pre class="bg-light p-2 mt-2 mb-0"><code>.\deploy-infra\deploy.ps1 -ResourceGroup "rg-name" -Location "uksouth" -DeployGenAI</code></pre>
                        </div>
                    }
                    else
                    {
                        <!-- Chat Messages Container -->
                        <div id="chatMessages" class="p-3" style="height: 500px; overflow-y: auto; background-color: #f8f9fa;">
                            <div class="text-center text-muted py-5">
                                <i class="bi bi-chat-dots" style="font-size: 3rem;"></i>
                                <p class="mt-3">Start a conversation! Ask me about expenses or request actions.</p>
                                <p class="small">Examples:</p>
                                <ul class="list-unstyled small">
                                    <li>"Show me all submitted expenses"</li>
                                    <li>"Create an expense for £50 for lunch on 2024-12-07"</li>
                                    <li>"What are the available categories?"</li>
                                </ul>
                            </div>
                        </div>
                    }
                </div>
                @if (Model.GenAIEnabled)
                {
                    <div class="card-footer bg-white">
                        <form id="chatForm" class="d-flex gap-2">
                            <input type="text" 
                                   id="userInput" 
                                   class="form-control" 
                                   placeholder="Type your message..." 
                                   autocomplete="off"
                                   required>
                            <button type="submit" 
                                    id="sendButton" 
                                    class="btn btn-primary">
                                <i class="bi bi-send"></i> Send
                            </button>
                        </form>
                        <div id="loadingIndicator" class="text-center mt-2" style="display: none;">
                            <div class="spinner-border spinner-border-sm text-primary" role="status">
                                <span class="visually-hidden">Loading...</span>
                            </div>
                            <span class="ms-2 small text-muted">Processing...</span>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @if (Model.GenAIEnabled)
    {
        <script>
            const chatMessages = document.getElementById('chatMessages');
            const chatForm = document.getElementById('chatForm');
            const userInput = document.getElementById('userInput');
            const sendButton = document.getElementById('sendButton');
            const loadingIndicator = document.getElementById('loadingIndicator');
            const conversationHistory = [];

            chatForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                
                const message = userInput.value.trim();
                if (!message) return;

                // Add user message to UI
                addMessageToChat('user', message);
                
                // Clear input
                userInput.value = '';
                
                // Disable input while processing
                userInput.disabled = true;
                sendButton.disabled = true;
                loadingIndicator.style.display = 'block';

                try {
                    // Send message to API
                    const response = await fetch('/api/chat', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({
                            message: message,
                            history: conversationHistory
                        })
                    });

                    const data = await response.json();
                    
                    if (data.success) {
                        addMessageToChat('assistant', data.response);
                    } else {
                        addMessageToChat('assistant', `Error: ${data.error || 'Unknown error occurred'}`);
                    }
                } catch (error) {
                    console.error('Error:', error);
                    addMessageToChat('assistant', 'Sorry, I encountered an error. Please try again.');
                } finally {
                    // Re-enable input
                    userInput.disabled = false;
                    sendButton.disabled = false;
                    loadingIndicator.style.display = 'none';
                    userInput.focus();
                }
            });

            function addMessageToChat(role, content) {
                // Store in conversation history
                conversationHistory.push({ role, content });

                // Remove the welcome message if this is the first message
                if (conversationHistory.length === 1) {
                    chatMessages.innerHTML = '';
                }

                // Create message element
                const messageDiv = document.createElement('div');
                messageDiv.className = `d-flex mb-3 ${role === 'user' ? 'justify-content-end' : 'justify-content-start'}`;
                
                const bubbleDiv = document.createElement('div');
                bubbleDiv.className = `p-3 rounded ${role === 'user' ? 'bg-primary text-white' : 'bg-white border'}`;
                bubbleDiv.style.maxWidth = '80%';
                
                const roleLabel = document.createElement('div');
                roleLabel.className = `small fw-bold mb-1 ${role === 'user' ? 'text-white-50' : 'text-muted'}`;
                roleLabel.textContent = role === 'user' ? 'You' : 'AI Assistant';
                
                const contentDiv = document.createElement('div');
                
                // Format the content (escape HTML first, then apply formatting)
                const formattedContent = formatMessage(content);
                contentDiv.innerHTML = formattedContent;
                
                bubbleDiv.appendChild(roleLabel);
                bubbleDiv.appendChild(contentDiv);
                messageDiv.appendChild(bubbleDiv);
                
                chatMessages.appendChild(messageDiv);
                
                // Scroll to bottom
                chatMessages.scrollTop = chatMessages.scrollHeight;
            }

            function formatMessage(text) {
                // Step 1: Escape HTML to prevent security issues
                let escaped = text
                    .replace(/&/g, '&amp;')
                    .replace(/</g, '&lt;')
                    .replace(/>/g, '&gt;')
                    .replace(/"/g, '&quot;')
                    .replace(/'/g, '&#039;');

                // Step 2: Apply formatting after escaping
                // Convert markdown-style bold (**text** or __text__)
                escaped = escaped.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
                escaped = escaped.replace(/__(.+?)__/g, '<strong>$1</strong>');

                // Convert line breaks
                escaped = escaped.replace(/\n/g, '<br>');

                // Convert numbered lists
                escaped = escaped.replace(/^(\d+)\.\s+(.+?)$/gm, '<li>$2</li>');
                
                // Wrap consecutive <li> in <ol>
                escaped = escaped.replace(/(<li>.*?<\/li>(<br>)?)+/gs, function(match) {
                    // Check if already wrapped in list tags
                    if (match.indexOf('<ol>') === -1 && match.indexOf('<ul>') === -1) {
                        return '<ol>' + match.replace(/<br>/g, '') + '</ol>';
                    }
                    return match;
                });

                // Convert bullet lists (-, *)
                escaped = escaped.replace(/^[-*]\s+(.+?)$/gm, '<li>$1</li>');
                
                // Wrap consecutive bullet <li> in <ul> if not already wrapped
                escaped = escaped.replace(/(<li>.*?<\/li>(<br>)?)+/gs, function(match) {
                    if (match.indexOf('<ol>') === -1 && match.indexOf('<ul>') === -1) {
                        return '<ul>' + match.replace(/<br>/g, '') + '</ul>';
                    }
                    return match;
                });

                return escaped;
            }
        </script>
    }
}

<style>
    #chatMessages::-webkit-scrollbar {
        width: 8px;
    }

    #chatMessages::-webkit-scrollbar-track {
        background: #f1f1f1;
    }

    #chatMessages::-webkit-scrollbar-thumb {
        background: #888;
        border-radius: 4px;
    }

    #chatMessages::-webkit-scrollbar-thumb:hover {
        background: #555;
    }

    #chatMessages ol, #chatMessages ul {
        margin-top: 0.5rem;
        margin-bottom: 0.5rem;
        padding-left: 1.5rem;
    }

    #chatMessages li {
        margin-bottom: 0.25rem;
    }
</style>
